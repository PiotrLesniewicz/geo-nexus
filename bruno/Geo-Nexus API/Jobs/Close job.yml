info:
  name: Close job
  type: http
  seq: 6
  tags:
    - Jobs

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/jobs/close?jobIdentifier=CRE000/00/01"
  params:
    - name: jobIdentifier
      value: CRE000/00/01
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/jobs/close"
      method: PATCH
      params:
        - name: jobIdentifier
          value: ""
          type: query
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

docs: Closes an active survey job.
