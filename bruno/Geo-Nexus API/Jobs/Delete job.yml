info:
  name: Delete job
  type: http
  seq: 11
  tags:
    - Jobs

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/jobs"
  body:
    type: json
    data: |-
      {
        "password": "GeoAdmin1!",
        "jobIdentifier": "CRE000/00/01"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/jobs"
      method: DELETE
      body:
        type: json
        data: |-
          {
            "password": "",
            "jobIdentifier": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

docs: Deletes a job and all associated reports. Requires password confirmation
