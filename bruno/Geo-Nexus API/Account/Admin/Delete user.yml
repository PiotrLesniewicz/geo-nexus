info:
  name: Delete user
  type: http
  seq: 2
  tags:
    - Account

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/companies/users/:email"
  params:
    - name: email
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/companies/users/:email"
      method: DELETE
      params:
        - name: email
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

docs: Soft-deletes a user by email within the authenticated admin's company
