info:
  name: Block company
  type: http
  seq: 1
  tags:
    - Account

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/companies/:nip/block"
  params:
    - name: nip
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/companies/:nip/block"
      method: PATCH
      params:
        - name: nip
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

docs: Deactivates a company by NIP. Only accessible by SUPER_ADMIN
